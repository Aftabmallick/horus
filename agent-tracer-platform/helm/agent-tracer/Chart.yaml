apiVersion: v2
name: agent-tracer
description: A Helm chart for Agent Tracer+ Enterprise Observability Platform
type: application
version: 0.1.0
appVersion: "1.0.0"

dependencies:
  # In a real enterprise deployment, we'd use Bitnami charts for these dependencies,
  # but for this standalone deployment, we will provision basic StatefulSets in our templates.
